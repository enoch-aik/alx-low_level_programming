#include "main.h"

/**
 * _abs - to print the absolute value of the number
 * @c: character representing the integer
 *
 * Return: check the code
 */

int _abs(int c)
{
	if (c < 0)
		return (-c);
	else
		return (c);
}
